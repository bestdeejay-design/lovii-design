# Примеры экранов LOVII по ролям

Готовые референс-страницы: как дизайн-система собирается в цельные экраны для каждой клиентской роли. Все значения — только из `../tokens/tokens.css` (подключается через `<link>` — примеры всегда тянут актуальный канон).

Каждый пример:
- подключает `tokens.css` и шрифт Inter 400–800;
- содержит anti-FOUC инлайн-скрипт **до** CSS, канонический `applyTheme`, переключатель темы;
- использует оба логотипа в DOM с CSS-переключением (`logo-light-img` / `logo-dark-img`);
- собран на капсуле 480px с брейкпоинтами 640/900/1024;
- проходит чек-лист `docs/checklist.md`.

## Каталог

| Файл | Роль | Что демонстрирует |
|---|---|---|
| `01-client-store.html` | **Клиент** — витрина | Хедер glass 56px, hero на `gradient-brand`, плитки категорий `tile-*`, карточка товара с бейджами `badge-*`, лента с `lv-edge-fade`, пилюли soft-пар, bottom-nav 64px → плавающая капсула ≥900px, toast |
| `02-business-dashboard.html` | **Бизнес** — дашборд | KPI-карточки (акцент `kpi-soft` + градиентный текст), сегменты-фильтры, CSS-гистограмма на градиентах канона, список заказов со статусами soft-пар, wide-сетка 1120px |
| `03-partner-portal.html` | **Партнёр** — портал | Статусная карточка на `gradient-ink` (текст только `on-brand`, золото — `--lv-sand`), прогресс-бар на брендовом градиенте, реферальная ссылка + копирование, выплаты со статусами, narrow 760px |
| `04-ambassador-program.html` | **Амбассадор** — программа | Мягкий hero на `hero-top`, промокод с градиентным текстом, уровни на `tile-*` подложках, интерактивный чек-лист, левитация `lv-float`, прогресс до уровня |
| `05-investor-overview.html` | **Инвестор** — обзор | Документный narrow-экран, hero на `gradient-ink`, метрики, таймлайн-роадмап на `hairline`, команда с аватарами `soft-*`, факты, CTA на `kpi-soft` |
| `06-ux-lab.html` | **UX-лаборатория** — паттерны v3 | Не ролевой экран: живые демо docs/10 — якорная лента с `scroll-margin-top`, кнопка «наверх» с порогом 480px, живой индикатор брейкпоинтов 640/900, карта состояний PWA-установки (standalone/iOS/prompt), hash-роутер на `#/…` с табами-ссылками |
| `07-voice-lab.html` | **Тон-лаборатория** — бренд и тон v4 | Не ролевой экран: живая демонстрация docs/11 — 5 принципов голоса с парами «пишем / не пишем», интерактивная матрица тона (успех/ошибка/пусто/загрузка/деньги) с каноническими тостами, копируемые формулировки состояний, юр.блок |

## Правила использования примеров

1. **Копируй механику, а не значения**: классы могут переименовываться под проект, но токены, пары `soft→*-text`, радиусы, отступы и тайминги — неизменны.
2. **Проверяй обе темы** после любых правок (переключатель в хедере каждого примера).
3. **Новые компоненты** сначала добавь в `docs/07-patterns.md` (сниппет) и при необходимости в style-guide, затем используй.
4. Примеры — справочные, не прод: их не деплоят, но их код должен проходить `docs/checklist.md` (для 06 — включая раздел 11 «UX-поведение», для 07 — раздел 12 «Текст и тон»).
